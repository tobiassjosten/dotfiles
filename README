## Installation

    $ bash < <(curl -s https://raw.github.com/tobiassjosten/dotfiles/master/install)

Install Vim bundles:

    $ vim +BundleInstall +qall

Install CtrlP C Matcher.

    $ cd vim/bundle/ctrlp-cmatcher/
    $ ./install_linux.sh
