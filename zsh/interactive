. ~/.shell/interactive

REPORTTIME=10

# Path to oh-my-zsh configuration.
ZSH=$HOME/.oh-my-zsh

# Set name of the theme to load.
ZSH_THEME="robbyrussell"

# Which plugins to load.
plugins=(bundler cabal capistrano command-not-found docker gem git sudo tmux yarn)

. $ZSH/oh-my-zsh.sh

. /usr/local/share/zsh-autosuggestions/zsh-autosuggestions.zsh

# Skip the "sure you want to delete all the files in" prompt.
setopt rmstarsilent
