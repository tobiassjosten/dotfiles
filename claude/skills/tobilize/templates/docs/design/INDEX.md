# Design

How one specific feature works — implementation knowledge tied to a single subsystem: how it is built and what to watch out for when working in it. If it answers *"how does this specific feature work?"*, it goes here. For patterns that apply system-wide, see [`../architecture/`](../architecture/INDEX.md); for the business rules a feature implements, see [`../domain/`](../domain/INDEX.md).

*No documents yet — add a file when a feature or module has a design worth recording. Update this index when you do.*
