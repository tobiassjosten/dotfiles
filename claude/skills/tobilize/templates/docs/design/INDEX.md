# Design

Feature-specific knowledge — how authentication is implemented, what to watch out for when working in the admin dashboard, the trade-offs behind the billing module. If it answers *"how does this specific feature work?"*, it goes here. For patterns that apply system-wide, see [`../architecture/`](../architecture/INDEX.md).

*No documents yet — add a file when a feature or module has a design worth recording. Update this index when you do.*
