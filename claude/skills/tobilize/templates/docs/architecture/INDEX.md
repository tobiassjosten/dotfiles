# Architecture

How the system is built — cross-cutting patterns and conventions that apply across the whole codebase: how dependencies are wired, how background jobs are processed, how the read and write models are split (CQRS), how logging and error handling work, how the layers are organized. If it answers *"how is X implemented here, in general?"*, it goes here. For knowledge tied to a specific feature, see [`../design/`](../design/INDEX.md); for ways of working rather than descriptions of the system, see [`../process/`](../process/INDEX.md).

*No documents yet — add files here as architectural patterns and structural decisions accumulate. Update this index when you do.*
