# Architecture

Cross-cutting patterns and conventions that apply across the whole codebase — how dependencies are wired, how background jobs are processed, how the read and write models are split (CQRS), how logging and error handling work, how the layers are organized. If it answers *"how do we do X here, in general?"*, it goes here. For knowledge tied to a specific feature, see [`../design/`](../design/INDEX.md).

*No documents yet — add files here as architectural patterns and structural decisions accumulate. Update this index when you do.*
